services:
  backend:
    build: ./server
    ports:
      - "3009:3009"
    env_file:
      - ./server/.env
    restart: unless-stopped

  frontend:
    build: ./client
    ports:
      - "3010:3010"
    depends_on:
      - backend
    restart: unless-stopped
